spring:
  application:
    name: file-service

  datasource:
    url: jdbc:postgresql://coursework-postgres:5432/postgres
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# not using
s3:
  datasource:
    url: "http://localhost:9000"
    user: "colddirol"
    password: "colddirol"
  buckets:
    pictureBucket: "pucture"
    videoBucket: "video"
